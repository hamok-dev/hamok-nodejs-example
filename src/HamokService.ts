import { HamokGrid } from "@hamok-dev/hamok-js-core";

export const grid = HamokGrid.builder().build();

export const localEndpointId = grid.localEndpointId;

export const transprot = grid.transport;

